import 'package:flutter/material.dart';

class ClassificacaoTab extends StatelessWidget {
  const ClassificacaoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Classificação',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
